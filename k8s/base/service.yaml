apiVersion: v1
kind: Service
metadata:
  name: data-ingestion-api
  namespace: data-ingestion
  labels:
    app: data-ingestion-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: data-ingestion-api
